server:
  port: 8085
spring:
  application:
    name: RECEPTION-QUERY-MS
#  profiles:
#    active: docker
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://keycloak-docker-compose:8080/realms/hisclientapp/protocol/openid-connect/certs
  datasource:
    url: jdbc:mysql://mysql-docker-compose:3306/reception_db?createDatabaseIfNotExist=true
    username: root
    password: myhawramRootPsw1
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
